When token is set in cookie and the user is not active when the user makes an action (request) check the status 
if is_active is false then distroy the cookie and redirect to '/'

# ------------------------
Product 
- AvailableSize
- AvailableColors
- Size
- Color
- quantity
- SKU