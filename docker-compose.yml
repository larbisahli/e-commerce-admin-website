version: '3.8'
services:
  admin_nextjs:
    container_name: admin-nextjs-app
    restart: always
    build: .
    ports:
      - '3001:3000'
    volumes:
      - /usr/src/app/node_modules
      - /usr/src/app/.next
networks:
  default:
    external:
      name: backend
